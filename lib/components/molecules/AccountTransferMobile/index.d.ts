import { IAccountTransferBody } from '../../atoms';
export declare const AccountTransferMobile: ({ id, deploy_hash, block_hash, antiquity, of, forValue, transference_id, amount, price, widthIcon, heightIcon }: IAccountTransferBody) => JSX.Element;
