import { IAccountTransferBody } from '../../atoms';
export declare const AccountTransferMobile: ({ deploy_hash, block_hash, antiquity, of, forValue, transference_id, amount, price, widthIcon, heightIcon, handleCopy }: IAccountTransferBody) => JSX.Element;
