import { IAccountDeployBody } from '../../atoms';
export declare const AccountDeployMobile: ({ id, deploy_hash, block_hash, antiquity, contract, amount, cost, price, widthIcon, heightIcon }: IAccountDeployBody) => JSX.Element;
