import { IAccountDeployBody } from '../../atoms';
export declare const AccountDeployMobile: ({ deploy_hash, block_hash, antiquity, contract, contractRedirect, handleCopy, entry_point, amount, cost, price, widthIcon, heightIcon }: IAccountDeployBody) => JSX.Element;
