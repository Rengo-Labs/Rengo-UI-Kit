import React from 'react';
import { IAccountDeployBody } from '../../atoms';
export declare const AccountDeployMobile: ({ deploy_hash, block_hash, antiquity, contract, contractRedirect, handleCopy, entry_point, amount, amountSymbol, cost, price, widthIcon, heightIcon, errorMessage }: IAccountDeployBody) => React.JSX.Element;
