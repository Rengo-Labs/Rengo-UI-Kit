import React from 'react';
import { ConnectionPopupProps } from '../WalletConnection/types';
export declare const ConnectionMobile: ({ closeCallback, linkCallback, wallets, isOpen }: ConnectionPopupProps) => React.JSX.Element;
