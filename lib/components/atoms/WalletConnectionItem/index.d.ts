import React from 'react';
import { Wallet } from '../../molecules/WalletConnection/types';
interface WalletConnectionProps {
    wallet: Wallet;
}
export declare const WalletConnectionItem: ({ wallet }: WalletConnectionProps) => React.JSX.Element;
export {};
