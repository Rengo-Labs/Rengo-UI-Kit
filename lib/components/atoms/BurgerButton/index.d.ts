import React from 'react';
export interface BurguerButtonProps {
    open: boolean;
    setOpen: any;
}
export declare const BurgerButton: ({ open, setOpen }: BurguerButtonProps) => React.JSX.Element;
